// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("symfony_dic", "for symfony_dic support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("symfony_dic", "enable symfony_dic support", "no");

if (PHP_SYMFONY_DIC != "no") {
	EXTENSION("symfony_dic", "symfony_dic.c");
}

